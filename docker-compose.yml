version: '3.1'
services:
  metrics-db:
    image: influxdb
    hostname: pipeline-metrics-db
    container_name: "pipeline-metrics-db"
    ports:
      - 3900:8086
    volumes:
      - influx_data:/var/lib/influxdb
      - influx_config:/etc/influxdb
    networks:
      - back_tier

  grafana:
    image: grafana/grafana
    hostname: pipeline-grafana
    container_name: "pipeline-grafana"
    ports:
      - 3910:3000
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: 1
      GF_AUTH_ANONYMOUS_ORG_NAME: "Janelia"
    volumes:
      - grafana_data:/var/lib/grafana
      - grafana_config:/etc/grafana
      - grafana_log:/var/log/grafana
    networks:
      - back_tier

  message-queue:
    image: rabbitmq:3-management
    container_name: "pipeline-message-queue"
    hostname: pipeline-message-queue
    ports:
      - 3982:5672
      - 3992:15672
    environment:
      RABBITMQ_ERLANG_COOKIE: "mqsecret"
    volumes:
      - mq_data:/var/lib/rabbitmq
    networks:
      - back_tier

  pipeline-db:
    image: postgres:9
    container_name: "pipeline-db"
    hostname: pipeline-db
    ports:
      - 3932:5432
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_PASSWORD: "pgsecret"
    volumes:
      - pipeline_data:/var/lib/postgresql/data
      - ./datastore/pipeline:/docker-entrypoint-initdb.d
    networks:
      - back_tier

  pipeline-client:
    image: mouselightpipeline/pipeline-client
    container_name: "pipeline-client"
    hostname: pipeline-client
    environment:
      PIPELINE_API_HOST: ${PIPELINE_API_HOST}
    ports:
      - 3100:3100
    networks:
      - back_tier

  vega-client:
    image: mouselightpipeline/worker-client
    container_name: "vega-client"
    hostname: vega-client
    environment:
      PIPELINE_WORKER_API_HOST: ${PIPELINE_VEGA_API_HOST}
      PIPELINE_WORKER_API_PORT: 3500
    ports:
      - 3600:3600
    networks:
      - back_tier

  vega-cluster-client:
    image: mouselightpipeline/worker-client
    container_name: "vega-cluster-client"
    hostname: vega-cluster-client
    environment:
      PIPELINE_WORKER_API_HOST: ${PIPELINE_VEGA_API_HOST}
      PIPELINE_WORKER_API_PORT: 3510
    ports:
      - 3610:3600
    networks:
      - back_tier

  sirius-client:
    image: mouselightpipeline/worker-client
    container_name: "sirius-client"
    hostname: sirius-client
    environment:
      PIPELINE_WORKER_API_HOST: ${PIPELINE_SIRIUS_API_HOST}
      PIPELINE_WORKER_API_PORT: 3500
    ports:
      - 3620:3600
    networks:
      - back_tier

  sirius-cluster-client:
    image: mouselightpipeline/worker-client
    container_name: "sirius-cluster-client"
    hostname: sirius-cluster-client
    environment:
      PIPELINE_WORKER_API_HOST: ${PIPELINE_SIRIUS_API_HOST}
      PIPELINE_WORKER_API_PORT: 3510
    ports:
      - 3630:3600
    networks:
      - back_tier

  canopus-client:
    image: mouselightpipeline/worker-client
    container_name: "canopus-client"
    hostname: canopus-client
    environment:
      PIPELINE_WORKER_API_HOST: ${PIPELINE_CANOPUS_API_HOST}
      PIPELINE_WORKER_API_PORT: 3500
    ports:
      - 3640:3600
    networks:
      - back_tier

  canopus-cluster-client:
    image: mouselightpipeline/worker-client
    container_name: "canopus-cluster-client"
    hostname: canopus-cluster-client
    environment:
      PIPELINE_WORKER_API_HOST: ${PIPELINE_CANOPUS_API_HOST}
      PIPELINE_WORKER_API_PORT: 3510
    ports:
      - 3650:3600
    networks:
      - back_tier

volumes:
  influx_data:
  influx_config:
  grafana_config:
  grafana_log:
  grafana_data:
  mq_data:
  pipeline_data:

networks:
  back_tier:
